# iOS Container Output

This directory is reserved for Safari Web Extension converter output (`.xcodeproj` and related app files).

The project is generated on macOS by:

```bash
bash scripts/ios/prepare-safari-project.sh
```

Generated Xcode project files are intentionally not committed by default in this repository.

